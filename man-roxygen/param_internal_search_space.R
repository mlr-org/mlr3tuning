#' @param internal_search_space ([`ParamSet`] or `NULL`)\cr
#'   The internal search space of the tuner. This includes parameters that the learner can optimize internally
#'   durign `$train()`, such as the number of epochs via early stopping.
