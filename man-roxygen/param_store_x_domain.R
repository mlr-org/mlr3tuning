#' @param store_x_domain (`logical(1)`)\cr
#'   If `TRUE` (default), (transformed) hyperparameter values are stored
#'   in archive as list items. Values are transformed if search space
#'   (`$search_space`) has a transformation function.
