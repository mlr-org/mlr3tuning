#' @param lgr_thresholds (named `character()` or `numeric()`)\cr
#' Thresholds for the `lgr` package e.g. c(rush = "debug", bbotk = "info", mlr3 = "info").
#' If `NULL`, no log messages from the workers are saved.
