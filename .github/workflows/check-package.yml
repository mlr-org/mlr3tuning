on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 4 * * *"

name: rcmdcheck

jobs:
  check-package:
    runs-on: ubuntu-latest

    steps:
    - uses: mlr-org/actions/rcmdcheck@rcmdcheck
