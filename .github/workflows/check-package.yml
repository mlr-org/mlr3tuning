on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "0 4 * * *"

name: rcmdcheck

jobs:
  check-package:
    runs-on: ubuntu-latest

    steps:
    - uses: mlr-org/actions/rcmdcheck@5db3e71bb446e4af66eae08cf709de548e070879
